<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/home_navigation"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="io.nemiron.meetgo.view.home.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_home_to_gradeTask"
            app:destination="@id/gradeTaskFragment" />
    </fragment>

    <fragment
        android:id="@+id/gradeTaskFragment"
        android:name="io.nemiron.meetgo.view.home.grade_task.GradeTaskFragment"
        android:label="fragment_grade_task"
        tools:layout="@layout/fragment_grade_task" />
</navigation>